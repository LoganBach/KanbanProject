## CPTS 489 Kanban Online Backend Server

### Instructions

- Install dependencies:
   ```bash
   npm install
   ```

- Start the development server:
   ```bash
   npm run dev
   ```

- The API will be available at `http://localhost:5000/api`. See [API README](API_README.md).

- The SQLite database will be created automatically at `./database.sqlite`
