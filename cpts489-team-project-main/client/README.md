## CPTS 489 Kanban Online Frontend Client

### Instructions

In the terminal:
- `npm install`
  - install dependencies
- `npm run dev`
  - Hosts development build (should run `vite --port 3000`).
- Find webpage at [localhost:3000](http://localhost:3000).